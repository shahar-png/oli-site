# Keep the alpha page out of search engines.
# Deliberately NOT done via robots.txt — a Disallow rule there would advertise
# the path to anyone who reads /robots.txt. An HTTP header does not.
/alpha/*
  X-Robots-Tag: noindex, nofollow, noarchive, nosnippet
